import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.shreeva.fyidonor',
  appName: 'FYI-Donor',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
